# Memory Map

## Policy
Store only distilled, durable signals:
- architecture decisions
- reusable patterns/checklists
- severe failures worth preventing

Do not store full plan/review/todo documents in memory. Store summaries that reference canonical files in `docs/` or `todos/`.
